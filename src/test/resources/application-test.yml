# Test profile configuration
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          # Disable JWT validation for tests (use mock security)
          issuer-uri: http://localhost:8080/auth/test

logging:
  level:
    root: INFO
    com.fidelity.integration.hub: DEBUG
