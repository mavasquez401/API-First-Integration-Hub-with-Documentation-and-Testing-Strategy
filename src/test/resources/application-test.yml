# Test profile configuration
integration:
  hub:
    security:
      # Disable JWT for tests; controller tests use @WithMockUser.
      enabled: false

logging:
  level:
    root: INFO
    com.fidelity.integration.hub: DEBUG
